<h1><%= @office.name %></h1>
<% @users.each do |user| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to "#{user.name}, #{user.field_of_exp}", user %>
      </h4>
    </div>
  </div>
<% end %>
